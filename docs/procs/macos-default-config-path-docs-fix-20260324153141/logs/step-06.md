## Step 6 — Create the final commit via `/commit`

### Actions Taken
- Staged only the macOS config path documentation fix, its draft/context references, and the proc artifacts.
- Created the final git commit with the planned message while preserving the repository's pre-commit hook execution.

### Verify Result
- `git commit -m 'docs: clarify macOS config path defaults'` completed successfully.
