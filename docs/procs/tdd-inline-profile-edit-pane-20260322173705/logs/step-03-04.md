## Step 3/4 — Inline edit entry and save flow
### Actions Taken
- Replaced the `[e]` path in `src/main.rs` so it enters a prefilled inline edit form instead of opening an external editor.
- Added save-flow helpers for add vs edit behavior, including unchanged-name allowance, duplicate rename rejection, update persistence, and post-save reload/reselect logic.
- Added focused `src/main.rs` tests for edit entry, duplicate-name validation, and reload/reselect behavior.

### Verify Result
- `rg -n "open_editor|KeyCode::Char\\('e'\\)" src/main.rs` showed no `open_editor` call on the `[e]` path.
- `cargo test e_key_enters_prefilled_edit_form -- --test-threads=1` exited 0.
- `cargo test edit_mode_ -- --test-threads=1` exited 0.
